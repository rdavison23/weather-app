# PostgreSQL connection string (replace with your own credentials)
DATABASE_URL=postgres://USERNAME:PASSWORD@HOST:PORT/weather_app

# Server port
PORT=5001

# Optional: Weather API key (if your project uses one)
# WEATHER_API_KEY=your_api_key_here